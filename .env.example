# Read-only RPCs used only by explicit verification commands. Never commit secrets.
ROBINHOOD_RPC_URL=
ROBINHOOD_RPC_URL_SECONDARY=

# Add only after Programmable publicly releases and authorizes the V4 API flow.
PROGRAMMABLE_API_KEY=
